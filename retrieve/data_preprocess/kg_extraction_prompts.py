# 实体提取提示词
ENTITY_EXTRACTION_PROMPT = """
请从以下文本中提取重要实体，并标注其类型。

文本内容：
{text}

请以JSON格式返回提取的实体，格式如下：
[
    {{
        "entity": "实体名称",
        "type": "实体类型"
    }},
    ...
]

实体类型应该包括：人物、组织、地点、时间、事件、产品、技术、船舶、项目。
请确保提取的实体在文本中确实存在，并且是重要的。
尽可能提取完整的实体名称，例如"韩国造船与海洋工程公司"而不是简单的"公司"。
"""

# 关系提取提示词
RELATIONSHIP_EXTRACTION_PROMPT = """
请从以下文本中提取实体之间的关系，构建知识图谱。

文本内容：
{text}

已识别的实体：
{entities}

请以JSON格式返回提取的关系，格式如下：
[
    {{
        "head": "头实体",
        "relation": "关系类型",
        "tail": "尾实体"
    }},
    ...
]

关系类型应该准确描述两个实体之间的联系，例如：
- 隶属于、创建了、参与了、位于、发生于、使用了、建造了、订购了
- 管理、投资了、担任了、合作了、签署合同、交付于、应用于、符合

请确保：
1. 提取的关系在文本中确实存在，不要推测
2. 关系描述应简洁明了，通常为2-4个字
3. 关系方向正确，例如"公司A 建造了 产品B"而不是"产品B 建造了 公司A"
4. 尽可能提取所有重要关系，构建完整的知识网络
"""